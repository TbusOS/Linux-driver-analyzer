# 异步机制演示驱动分析

本示例展示了 Linux 内核中各种**异步处理机制**的使用方式。

## 📄 源代码

- [async_demo_example.c](async_demo_example.c) - 320行示例代码

## 📊 分析结果

| 分析器 | 结果文件 | 说明 |
|--------|----------|------|
| basic_analyzer | [basic_analysis.json](basic_analysis.json) | 函数调用流 + 异步机制识别 |
| advanced_analyzer | [advanced_analysis.json](advanced_analysis.json) | 结构体关系 + 调用图 |

## 🔬 分析摘要

### 识别的异步机制

| 类型 | 函数 | 执行上下文 |
|------|------|------------|
| ⚙️ 工作队列 | `demo_work_handler()` | 进程上下文，可睡眠 |
| ⏰ 延迟工作 | `demo_delayed_work_handler()` | 进程上下文，可睡眠 |
| 🔄 Tasklet | `demo_tasklet_handler()` | 软中断上下文，不可睡眠 |
| ⏲️ 定时器 | `demo_timer_callback()` | 软中断上下文，不可睡眠 |
| ⏱️ 高精度定时器 | `demo_hrtimer_callback()` | 硬中断上下文 |
| 🧵 内核线程 | `demo_kthread_func()` | 进程上下文，可睡眠 |

### 结构体关系

```
async_demo_dev
    ├── work_struct        (工作队列)
    ├── delayed_work       (延迟工作)
    ├── tasklet_struct     (Tasklet)
    ├── timer_list         (普通定时器)
    ├── hrtimer            (高精度定时器)
    └── completion         (完成量)
```

### 入口点函数

| 入口点 | 函数 | 触发时机 |
|--------|------|----------|
| 🚀 module_init | `async_demo_init()` | 模块加载 |
| 🛑 module_exit | `async_demo_exit()` | 模块卸载 |
| 🔌 probe | `async_demo_probe()` | 设备匹配 |
| ⏏️ remove | `async_demo_remove()` | 设备移除 |

## 👀 可视化查看

1. 打开可视化工具：

```bash
# 方法1：直接打开
open ../../web/templates/call_flow_viewer.html

# 方法2：启动本地服务器
cd ../..
python -m http.server 8080
# 访问 http://localhost:8080/web/templates/call_flow_viewer.html
```

2. 点击 **"导入JSON"** 按钮
3. 选择 `basic_analysis.json` 或 `advanced_analysis.json`
4. 浏览分析结果

## 🎓 学习要点

这个示例展示了：

1. **不同执行上下文的区别**
   - 进程上下文：可以睡眠，使用互斥锁
   - 软中断上下文：不可睡眠，只能用自旋锁
   - 中断上下文：必须快速返回

2. **异步机制的选择**
   - 需要睡眠 → 工作队列或内核线程
   - 不需要睡眠，快速处理 → Tasklet
   - 定时任务 → 定时器

3. **上下半部机制**
   - 硬中断处理器做最少工作
   - 耗时操作放到下半部（Tasklet/工作队列）

