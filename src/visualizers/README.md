# 📊 可视化生成器

本目录用于存放可视化输出生成器。

## 📋 规划中的生成器

| 生成器 | 说明 | 状态 |
|--------|------|------|
| HTML | 交互式网页 | ✅ 已有（web目录） |
| DOT | Graphviz 格式 | 📅 计划中 |
| Mermaid | Markdown 图表 | 📅 计划中 |
| PlantUML | UML 图 | 📅 计划中 |

## 🎯 设计目标

将分析结果转换为各种可视化格式：

```
分析结果 JSON
     │
     ├──▶ HTML (交互式)
     ├──▶ DOT (Graphviz)
     ├──▶ Mermaid (Markdown)
     └──▶ PlantUML (UML)
```

## 📝 生成器接口

```python
class Visualizer:
    def generate(self, analysis_data: dict) -> str:
        """将分析结果转换为可视化格式"""
        pass
    
    def save(self, output_path: str):
        """保存到文件"""
        pass
```

## 🌐 当前实现

目前可视化功能在 `web/templates/` 目录下实现：

- `call_flow_viewer.html` - 函数调用流可视化
- `struct_viewer.html` - 结构体关系可视化

未来计划将可视化逻辑抽取到此目录，实现多格式输出。

