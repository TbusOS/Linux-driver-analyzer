# 🎯 核心分析模块

本目录包含 LDA 的核心分析引擎。

## 📄 文件说明

| 文件 | 说明 |
|------|------|
| `basic_analyzer.py` | 基础分析器 - 正则匹配 + 知识库 |
| `advanced_analyzer.py` | 高级分析器 - 结构体解析 + 调用图 |
| `knowledge_base.json` | Linux内核API知识库 |

## 🔬 basic_analyzer.py

### 功能

- ✅ 函数定义识别
- ✅ 函数调用提取
- ✅ 操作结构体识别（`usb_driver`, `file_operations` 等）
- ✅ 回调函数映射
- ✅ 异步机制识别

### 识别的异步机制

| 类型 | 初始化宏/函数 | 执行上下文 |
|------|---------------|------------|
| 工作队列 | `INIT_WORK` | 进程上下文，可睡眠 |
| 延迟工作 | `INIT_DELAYED_WORK` | 进程上下文，可睡眠 |
| Tasklet | `tasklet_init` | 软中断上下文 |
| 定时器 | `timer_setup` | 软中断上下文 |
| 高精度定时器 | `hrtimer_init` | 硬中断上下文 |
| 内核线程 | `kthread_run` | 进程上下文，可睡眠 |

### 用法

```bash
python basic_analyzer.py <源文件.c> [-o 输出.json]
```

## 🔬 advanced_analyzer.py

### 功能

- ✅ 结构体定义解析
- ✅ 字段类型和指针识别
- ✅ 函数指针字段识别
- ✅ 结构体嵌套关系
- ✅ 函数参数解析

### 用法

```bash
python advanced_analyzer.py <源文件.c> --structs [-o 输出.json]
```

## 📚 knowledge_base.json

Linux内核知识库结构：

```json
{
  "driver_frameworks": {
    "usb_driver": {
      "callbacks": {
        "probe": { "trigger": "设备匹配时", "context": "进程上下文" },
        "disconnect": { "trigger": "设备移除时", "context": "进程上下文" }
      }
    }
  },
  "async_mechanisms": {
    "work_struct": { "context": "进程上下文，可睡眠" }
  }
}
```

## 🧪 测试

```bash
# 运行测试
pytest tests/test_basic_analyzer.py -v
```

